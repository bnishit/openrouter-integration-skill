interface:
  display_name: "OpenRouter Skill"
  short_description: "OpenRouter models, providers, generation cost lookup, tools, and routing"
  default_prompt: "Use $openrouter-integration to add OpenRouter model discovery, provider discovery, free-model filtering, generation cost lookup, chat completions, multimodal support, and routing to this project."
